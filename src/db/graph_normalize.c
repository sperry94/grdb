#include <assert.h>
#include <stdio.h>
#include <string.h>
#include "graph.h"

void
graph_normalize(graph_t in, graph_t out)
{
  /*vertex_list_t vtx_list;

  vertex_t v;
  for(v = in->v; v != NULL; v=v->next)
  {
    vtx_list.id = v->id;
    vtx_list
  }

  edge_t e;
  for(e = in->e; e != NULL; e=e->next)
  {

  }*/
}