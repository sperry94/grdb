#include <assert.h>
#include <stdio.h>
#include <string.h>
#include "graph.h"

void
graph_denormalize(graph_t in, graph_t out)
{
  vertex_t v;
  for(v=in->v; v != NULL; v=v->next)
  {
    
  }

  edge_t e;
  for(e=in->e; e != NULL; e=e->next)
  {

  }
}